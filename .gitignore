# Built Roku sideload package
roku-channel/now-playing-vinyl.zip

# Node (if any piece ever needs it)
node_modules/
*.log

# Env / secrets (AudD API token, etc.)
.env

# Relay runtime state
relay/now_playing.json
relay/now_playing.json.tmp
__pycache__/
