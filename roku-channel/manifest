title=Now Playing Vinyl
major_version=0
minor_version=3
build_version=1
ui_resolutions=fhd

# Bump build_version on every re-sideload of unchanged code - the web
# installer treats an identical package as a no-op and won't relaunch.
#
# relay_url / poll_interval_seconds are NOT read from here - they're
# hardcoded constants at the top of components/MainScene.brs. (An
# earlier version tried roAppInfo.GetValue() for custom manifest keys;
# that's only documented for Roku's well-known keys and silently
# returned "" here, which is why edits belong in MainScene.brs instead.)
